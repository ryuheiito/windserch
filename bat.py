import subprocess

# 1_wind.pyの実行
subprocess.run(["python", "1_wind.py"])

# 2_wind.pyの実行
subprocess.run(["python", "2_wind.py"])

# 3_wind.pyの実行
subprocess.run(["python", "3_wind.py"])

# 4_wind.pyの実行
subprocess.run(["python", "4_wind.py"])

# 5_wind.pyの実行
subprocess.run(["python", "5_wind.py"])
